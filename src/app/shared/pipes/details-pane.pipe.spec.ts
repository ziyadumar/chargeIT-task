import { DetailsPanePipe } from './details-pane.pipe';

describe('DetailsPanePipe', () => {
  it('create an instance', () => {
    const pipe = new DetailsPanePipe();
    expect(pipe).toBeTruthy();
  });
});
