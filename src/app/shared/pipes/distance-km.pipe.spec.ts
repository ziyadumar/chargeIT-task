import { DistanceKmPipe } from './distance-km.pipe';

describe('DistanceKmPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceKmPipe();
    expect(pipe).toBeTruthy();
  });
});
